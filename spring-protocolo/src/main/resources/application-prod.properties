spring.datasource.url=${DB_PROTOCOL_URL}
spring.datasource.username=${DB_PROTOCOL_USER}
spring.datasource.password=${DB_PROTOCOL_PASSWORD}

logging.file.name=${LOG_PROTOCOL_PATH:logs/logs-protocolo.txt}

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}